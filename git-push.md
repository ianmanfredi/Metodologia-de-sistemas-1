# Subir cambios: git push

Su funcion es subir los archivos al repo.
Cuando ya guardaste los archivos, usas **git push** para mandar todo al repositorio de github para que todos puedan verlo.

### Ejemplo de uso:
```bash
git push -u origin feat-comandos-locales
git push